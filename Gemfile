source "https://rubygems.org"

gem "rubst_api", "~> 0.1.0"

# RubstAPI 0.1.0 uses Base64 security helpers. Ruby 4 ships base64 separately.
# This can be removed after upgrading to a RubstAPI release that declares it.
gem "base64", ">= 0.3"
